# My Scripts files

Ten katalog jest lokalną, testowalną kopią źródeł `MeshCentral-MyScripts`.

Wypełnij lub odśwież go poleceniem:

```powershell
npm run sync:files
```

Skrypt kopiuje pełne źródła z `legacy/myscripts`, z wyłączeniem `.git`, `node_modules`, danych runtime i logów.
