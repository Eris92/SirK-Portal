"use strict";

// Deterministic migration for the host-neutral SIRK Portal runtime.
var fs = require("fs");
var path = require("path");
var root = path.resolve(__dirname, "..");
function file(name) { return path.join(root, name); }
function read(name) { return fs.readFileSync(file(name), "utf8"); }
function write(name, value) { fs.mkdirSync(path.dirname(file(name)), { recursive: true }); fs.writeFileSync(file(name), value, "utf8"); }
function replace(name, from, to) { var value = read(name); if (value.indexOf(from) < 0) throw new Error("Missing marker in " + name + ": " + from); write(name, value.replace(from, to)); }

write("server/contracts/host-context.js", `"use strict";

function required(value, name) {
    if (!value) throw new Error("HostContext requires " + name + ".");
    return value;
}
function normalizeUser(user) {
    user = user || {};
    return { id: String(user.id || user._id || ""), displayName: String(user.displayName || user.realname || user.name || user._id || "unknown"), tenantId: String(user.tenantId || user.domain || ""), roles: Array.isArray(user.roles) ? user.roles.map(String) : [], groups: Array.isArray(user.groups) ? user.groups.map(String) : [], isAdmin: user.isAdmin === true || user.siteadmin === true || Number(user.siteadmin) === 0xFFFFFFFF || (Number(user.siteadmin) | 0) === -1, raw: user.raw || user };
}
function createHostContext(options) {
    options = options || {};
    var context = { kind: String(options.kind || "standalone"), dataRoot: required(options.dataRoot, "dataRoot"), fs: required(options.fs || require("fs"), "fs"), path: required(options.path || require("path"), "path"), logger: options.logger || console, auth: required(options.auth, "auth provider"), devices: required(options.devices, "device provider"), sessions: required(options.sessions, "session provider"), events: options.events || { publish: function () { return false; } }, capabilities: Object.assign({ devices: true, desktop: false, terminal: false, files: false, nativeUi: false, extensions: true }, options.capabilities || {}), legacyParent: options.legacyParent || null };
    context.normalizeUser = normalizeUser;
    context.currentUser = function (req) { return Promise.resolve(context.auth.currentUser(req)).then(normalizeUser); };
    return context;
}
module.exports = { createHostContext: createHostContext, normalizeUser: normalizeUser };
`);
write("server/core/module-registry.js", `"use strict";
var fs=require("fs"),path=require("path");
var BUILTIN=[{key:"approvalcenter",name:"Approvals",modulePath:"../modules/approval-center/index.js"},{key:"moverequests",name:"Move Requests",modulePath:"../modules/move-requests/index.js"},{key:"mycommands",name:"Commands",modulePath:"../modules/commands/index.js"},{key:"myjira",name:"Jira Integration",modulePath:"../modules/jira/index.js"},{key:"defendertools",name:"Security",modulePath:"../modules/security/index.js"},{key:"myscripts",name:"Automation",modulePath:"../modules/automation/index.js"}];
function safeKey(v){return /^[a-z][a-z0-9_-]{1,63}$/i.test(String(v||""))?String(v):"";}
function discoverExtensions(dataRoot){var directory=path.join(dataRoot,"extensions");if(!fs.existsSync(directory))return[];return fs.readdirSync(directory,{withFileTypes:true}).filter(function(e){return e.isDirectory();}).map(function(e){var r=path.join(directory,e.name),m=path.join(r,"sirk-module.json");if(!fs.existsSync(m))return null;var x=JSON.parse(fs.readFileSync(m,"utf8")),k=safeKey(x.key||e.name);if(!k)throw Error("Invalid extension key in "+m);var entry=path.resolve(r,String(x.serverEntry||"index.js"));if(entry.indexOf(r+path.sep)!==0)throw Error("Extension entry escapes its directory: "+k);return{key:k,name:String(x.name||k),modulePath:entry,external:true,manifest:x};}).filter(Boolean);}
module.exports={builtins:BUILTIN,discoverExtensions:discoverExtensions,descriptors:function(dataRoot){return BUILTIN.concat(discoverExtensions(dataRoot));}};
`);
write("server/adapters/standalone/index.js", `"use strict";
var fs=require("fs"),path=require("path"),contract=require("../../contracts/host-context.js");
function user(){return{id:process.env.SIRK_USER_ID||"local/admin",displayName:process.env.SIRK_USER_NAME||"Local Administrator",tenantId:process.env.SIRK_TENANT_ID||"local",roles:["admin"],groups:[],isAdmin:true};}
module.exports.createHost=function(o){o=o||{};var dataRoot=path.resolve(o.dataRoot||process.env.SIRK_DATA_ROOT||path.join(process.cwd(),"sirk-platform-data"));fs.mkdirSync(path.join(dataRoot,"extensions"),{recursive:true});return contract.createHostContext({kind:"standalone",dataRoot:dataRoot,fs:fs,path:path,auth:o.auth||{currentUser:function(){return user();}},devices:o.devices||{list:function(){return Promise.resolve({meshes:[],nodes:[]});},resolve:function(){return Promise.reject(Error("No device connector is configured."));},runCommand:function(){return Promise.reject(Error("No agent transport is configured."));}},sessions:o.sessions||{capabilities:{desktop:false,terminal:false,files:false},create:function(){return Promise.reject(Error("No remote-session connector is configured."));}},capabilities:Object.assign({devices:true,desktop:false,terminal:false,files:false,nativeUi:false,extensions:true},o.capabilities||{}),logger:o.logger||console});};
`);
write("server/adapters/meshcentral/index.js", `"use strict";
var path=require("path"),contract=require("../../contracts/host-context.js"),deviceFactory=require("../../core/device-service.js"),shared=require("../../core/shared.js");
module.exports.createHost=function(o){o=o||{};var parent=o.parent,mesh=parent&&parent.parent,fs=parent&&parent.fs||require("fs"),p=parent&&parent.path||path,dataBase=mesh&&mesh.datapath?mesh.datapath:p.dirname(parent&&parent.pluginPath||o.pluginRoot),legacy=deviceFactory.createDeviceService({parent:parent,source:o.source});return contract.createHostContext({kind:"meshcentral",dataRoot:p.join(dataBase,"sirk-platform-data"),fs:fs,path:p,legacyParent:parent,auth:{currentUser:function(req){return req&&(req.sirkUser||req.user)||null;}},devices:{list:function(user){return legacy.visibleNodes(user&&user.raw||user);},resolve:function(user,nodeId,settings){return legacy.resolveNode(user&&user.raw||user,nodeId,settings);},runCommand:function(ctx,cmd,responseId,sessionId){return legacy.sendRunCommands(ctx,cmd,responseId,sessionId);},legacy:legacy},sessions:{capabilities:{desktop:true,terminal:true,files:true},create:function(kind,nodeId){return Promise.resolve({kind:kind,nodeId:nodeId,mode:"meshcentral-native"});}},events:{publish:function(targets,event){return shared.dispatch(parent,o.source,targets,event);}},capabilities:{devices:true,desktop:true,terminal:true,files:true,nativeUi:true,extensions:true},logger:console});};
`);
write("server/http/api-router.js", `"use strict";
function json(res,status,value){res.statusCode=status;res.setHeader("Content-Type","application/json; charset=utf-8");res.setHeader("Cache-Control","no-store");res.end(JSON.stringify(value));}
module.exports.createApiHandler=function(runtime,host){return function(req,res){var url=new URL(req.url,"http://sirk.local"),match=url.pathname.match(/^\\/api\\/modules\\/([^/]+)\\/([^/]+)$/);Promise.resolve(host.currentUser(req)).then(function(user){if(url.pathname==="/api/bootstrap"&&req.method==="GET"){var value=runtime.bootstrap(user.raw||user);value.host={kind:host.kind,capabilities:host.capabilities};json(res,200,value);return;}if(!match){json(res,404,{ok:false,error:"Endpoint not found."});return;}var fake={method:req.method,query:Object.fromEntries(url.searchParams.entries()),headers:req.headers,body:{}};runtime.request(req.method,decodeURIComponent(match[1]),decodeURIComponent(match[2]),fake,{statusCode:200,setHeader:function(n,v){res.setHeader(n,v);},set:function(n,v){res.setHeader(n,v);},status:function(c){this.statusCode=c;return this;},send:function(body){res.statusCode=this.statusCode;res.end(body);},end:function(body){res.statusCode=this.statusCode;res.end(body);}},user.raw||user);}).catch(function(e){json(res,401,{ok:false,error:String(e&&e.message||e)});});};};
`);
write("server/standalone.js", `"use strict";
var http=require("http"),fs=require("fs"),path=require("path"),adapter=require("./adapters/standalone/index.js"),runtimeFactory=require("./core/runtime-portal.js"),apiFactory=require("./http/api-router.js");
function type(n){if(/\\.css$/i.test(n))return"text/css; charset=utf-8";if(/\\.js$/i.test(n))return"text/javascript; charset=utf-8";if(/\\.json$/i.test(n))return"application/json; charset=utf-8";if(/\\.svg$/i.test(n))return"image/svg+xml";if(/\\.png$/i.test(n))return"image/png";return"application/octet-stream";}
function start(o){o=o||{};var root=path.resolve(__dirname,".."),host=adapter.createHost(o),runtime=runtimeFactory.createRuntime({host:host,pluginRoot:root,source:{shortName:"SIRKPortalStandalone"}}),api=apiFactory.createApiHandler(runtime,host);return Promise.resolve(runtime.initialize()).then(function(){var server=http.createServer(function(req,res){var url=new URL(req.url,"http://sirk.local");if(url.pathname.indexOf("/api/")===0){api(req,res);return;}if(url.pathname==="/"||url.pathname==="/sirkportal/"){var version=require("../config.json").version,html=fs.readFileSync(path.join(root,"public/portal/standalone/index.html"),"utf8").replace(/__API_BASE_JSON__/g,JSON.stringify("/api")).replace(/__PLUGIN_PIN_JSON__/g,JSON.stringify("")).replace(/__ASSET_BASE_JSON__/g,JSON.stringify("/assets")).replace(/__NATIVE_URL_JSON__/g,JSON.stringify("")).replace(/__LOGOUT_URL_JSON__/g,JSON.stringify("/auth/logout")).replace(/__USER_IMAGE_URL_JSON__/g,JSON.stringify("/api/user/image")).replace(/__DEFAULT_USER_IMAGE_URL_JSON__/g,JSON.stringify("/assets/images/user-256.png")).replace(/__VERSION_JSON__/g,JSON.stringify(version)).replace(/__ASSET_BASE__/g,"/assets").replace(/__NATIVE_URL__/g,"").replace(/__VERSION__/g,version);res.setHeader("Content-Type","text/html; charset=utf-8");res.end(html);return;}if(url.pathname.indexOf("/assets/")===0){var rel=decodeURIComponent(url.pathname.slice(8));if(rel.indexOf("..")>=0){res.statusCode=400;res.end("Bad path");return;}var c=[path.join(root,"public/portal",rel),path.join(root,"public/shared",rel),path.join(root,"public",rel),path.join(root,"assets",rel)],target=c.find(function(x){return fs.existsSync(x)&&fs.statSync(x).isFile();});if(!target){res.statusCode=404;res.end("Not found");return;}res.setHeader("Content-Type",type(target));fs.createReadStream(target).pipe(res);return;}res.statusCode=404;res.end("Not found");});var port=Number(o.port||process.env.PORT||8080);return new Promise(function(resolve){server.listen(port,o.host||process.env.HOST||"127.0.0.1",function(){resolve(server);});});});}
if(require.main===module)start().then(function(s){console.log("SIRK Portal standalone listening on",s.address());}).catch(function(e){console.error(e);process.exitCode=1;});module.exports={start:start};
`);
replace("plugin-main.js",'var createAdmin = require("./admin.js").admin;\nvar VERSION = require("./config.json").version;','var createAdmin = require("./admin.js").admin;\nvar VERSION = require("./config.json").version;\nvar meshHostFactory = require("./server/adapters/meshcentral/index.js");');
replace("plugin-main.js",'        obj.runtime = require("./server/core/runtime-portal.js").createRuntime({\n            parent: parent,\n            pluginRoot: __dirname,\n            source: obj\n        });','        obj.host = meshHostFactory.createHost({ parent: parent, pluginRoot: __dirname, source: obj });\n        obj.runtime = require("./server/core/runtime-portal.js").createRuntime({\n            host: obj.host,\n            parent: parent,\n            pluginRoot: __dirname,\n            source: obj\n        });');
replace("server/core/runtime.js",'var folderAccess = require("./folder-access.js");','var folderAccess = require("./folder-access.js");\nvar moduleRegistry = require("./module-registry.js");');
replace("server/core/runtime.js",'    var parent = options.parent;\n    var pluginRoot = options.pluginRoot;\n    var fs = parent.fs || require("fs");\n    var nativePath = parent.path || require("path");\n    var meshServer = parent.parent;\n    var dataBase = meshServer && meshServer.datapath\n        ? meshServer.datapath\n        : nativePath.dirname(parent.pluginPath || pluginRoot);\n    var dataRoot = nativePath.join(dataBase, "sirk-platform-data");','    var host = options.host || null;\n    var parent = options.parent || host && host.legacyParent || {};\n    var pluginRoot = options.pluginRoot;\n    var fs = host && host.fs || parent.fs || require("fs");\n    var nativePath = host && host.path || parent.path || require("path");\n    var dataRoot = host && host.dataRoot || nativePath.join(nativePath.dirname(parent.pluginPath || pluginRoot), "sirk-platform-data");');
replace("server/core/runtime.js",'    context.device = deviceFactory.createDeviceService({ parent: parent, source: options.source });','    context.host = host;\n    context.capabilities = host && host.capabilities || {};\n    context.device = host && host.devices || deviceFactory.createDeviceService({ parent: parent, source: options.source });');
replace("server/core/runtime.js",'    MODULES.forEach(function (descriptor) {','    moduleRegistry.descriptors(dataRoot).forEach(function (descriptor) {');
replace("server/core/runtime.js",'            var factory = require(descriptor.path);','            var factory = require(descriptor.modulePath || descriptor.path);');
replace("server/core/runtime-portal.js",'        return {\n            ok: true,\n            version: VERSION,','        return {\n            ok: true,\n            version: VERSION,\n            host: { kind: context.host && context.host.kind || "meshcentral", capabilities: context.capabilities || {} },');
replace("public/portal/standalone/scripts/core.js",'        var endpoint = new URL(String(window.__SIRK_PLATFORM_API_BASE__ || "pluginadmin.ashx"), window.location.href);\n        endpoint.searchParams.set("pin", "SIRKPortal");\n        if (moduleName) endpoint.searchParams.set("module", moduleName);\n        if (assetName) endpoint.searchParams.set("asset", assetName);','        var base = String(window.__SIRK_PLATFORM_API_BASE__ || "/api");\n        var pluginMode = /pluginadmin\\.ashx(?:$|\\?)/i.test(base);\n        var endpoint;\n        if (pluginMode) {\n            endpoint = new URL(base, window.location.href);\n            endpoint.searchParams.set("pin", String(window.__SIRK_PLATFORM_PLUGIN_PIN__ || "SIRKPortal"));\n            if (moduleName) endpoint.searchParams.set("module", moduleName);\n            if (assetName) endpoint.searchParams.set("asset", assetName);\n        } else {\n            base = base.replace(/\\/$/, "");\n            if (!moduleName && assetName === "bootstrap") endpoint = new URL(base + "/bootstrap", window.location.href);\n            else endpoint = new URL(base + "/modules/" + encodeURIComponent(moduleName || "_runtime") + "/" + encodeURIComponent(assetName || "index"), window.location.href);\n        }');
replace("plugin-main-standalone.js",'            "__API_BASE_JSON__": JSON.stringify(base + "pluginadmin.ashx"),','            "__API_BASE_JSON__": JSON.stringify(base + "pluginadmin.ashx"),\n            "__PLUGIN_PIN_JSON__": JSON.stringify("SIRKPortal"),');
replace("public/portal/standalone/index.html",'window.__SIRK_PLATFORM_API_BASE__ = __API_BASE_JSON__;','window.__SIRK_PLATFORM_API_BASE__ = __API_BASE_JSON__;\n        window.__SIRK_PLATFORM_PLUGIN_PIN__ = typeof __PLUGIN_PIN_JSON__ === "undefined" ? "" : __PLUGIN_PIN_JSON__;');
var pkg=JSON.parse(read("package.json"));pkg.version="1.6.0";pkg.scripts.start="node server/standalone.js";pkg.scripts["test:standalone"]="node test/standalone-host-boundary.test.js";if(pkg.scripts.test.indexOf("standalone-host-boundary.test.js")<0)pkg.scripts.test=pkg.scripts.test.replace("node test/security.test.js","node test/standalone-host-boundary.test.js && node test/security.test.js");write("package.json",JSON.stringify(pkg,null,2)+"\n");var config=JSON.parse(read("config.json"));config.version="1.6.0";write("config.json",JSON.stringify(config,null,2)+"\n");write("README.md",read("README.md").replace(/^# SIRK Management Platform [^\r\n]+/m,"# SIRK Management Platform 1.6.0"));var changelog=read("changelog.md");if(changelog.indexOf("## 1.6.0")<0)changelog="## 1.6.0\n\n- Introduced a host-neutral runtime contract and isolated MeshCentral behind an adapter.\n- Added a standalone Node.js server and REST API mode without `pluginadmin.ashx` or plugin pins.\n- Added standalone authentication, device and remote-session provider contracts.\n- Added an application-owned extension registry under `sirk-platform-data/extensions`.\n- Exposed host capabilities for Desktop, Terminal, Files and native UI selection.\n\n"+changelog;write("changelog.md",changelog);var history=JSON.parse(read("version-history.json")),item={version:"1.6.0",date:"2026-07-24",changes:["Add host-neutral runtime and adapters","Add standalone HTTP server and REST API","Add application extension registry","Expose remote-session capabilities"]};if(Array.isArray(history))history.unshift(item);else history.versions.unshift(item);write("version-history.json",JSON.stringify(history,null,2)+"\n");
write("test/standalone-host-boundary.test.js",`"use strict";\nvar assert=require("assert"),fs=require("fs"),path=require("path"),root=path.join(__dirname,"..");var standalone=require("../server/adapters/standalone/index.js"),host=standalone.createHost({dataRoot:path.join(root,".tmp-standalone-test")});assert.strictEqual(host.kind,"standalone");assert.strictEqual(host.capabilities.nativeUi,false);assert.strictEqual(typeof host.devices.list,"function");assert.strictEqual(typeof host.sessions.create,"function");assert.strictEqual(typeof require("../server/standalone.js").start,"function");var core=fs.readFileSync(path.join(root,"public/portal/standalone/scripts/core.js"),"utf8");assert.ok(core.indexOf("/modules/")>=0);assert.ok(core.indexOf("__SIRK_PLATFORM_PLUGIN_PIN__")>=0);var runtime=fs.readFileSync(path.join(root,"server/core/runtime.js"),"utf8");assert.ok(runtime.indexOf("options.host")>=0);assert.ok(fs.existsSync(path.join(root,"server/core/module-registry.js")));fs.rmSync(path.join(root,".tmp-standalone-test"),{recursive:true,force:true});console.log("Standalone host boundary: OK");\n`);
console.log("Applied standalone host boundary 1.6.0");
